apiVersion: apps/v1
kind: Deployment
metadata:
  name: crypto-frontend-deployment
  namespace: crypto-project
  labels:
    app: crypto-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crypto-frontend
  template:
    metadata:
      labels:
        app: crypto-frontend
    spec:
      containers:
      - name: frontend-service
        image: fgrothaus1/crypto-frontend:73aff277a6bd3a335e6a0a9a1894b8d91d6cb66d
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: web-port
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"