apiVersion: v1
kind: Service
metadata:
  name: crypto-api-service
  namespace: default
spec:
  selector:
    app: crypto-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: api-port
  type: ClusterIP