apiVersion: apps/v1
kind: Deployment
metadata:
  name: crypto-consumer-deployment
  namespace: crypto-project
  labels:
    app: crypto-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crypto-consumer
  template:
    metadata:
      labels:
        app: crypto-consumer
    spec:
      containers:
      - name: consumer-service
        image: fgrothaus1/crypto-backend:45844825bf133194da199496881aa2c38c370c57
        imagePullPolicy: Always
        command: ["python", "consumer.py"]
        envFrom:
        - configMapRef:
            name: backend-config
        - secretRef:
            name: postgres-secret
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"