apiVersion: apps/v1
kind: Deployment
metadata:
  name: crypto-ingestion-deployment
  namespace: crypto-project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crypto-ingestion
  template:
    metadata:
      labels:
        app: crypto-ingestion
    spec:
      containers:
      - name: ingestion-service
        image: fgrothaus1/crypto-ingestion:b50375b500b57454a83084e8948f57babcda7fa1
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: ingestion-config
        - secretRef:
            name: ingestion-secret # untracked file. Wird lokal mit kubectl eingebunden
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"